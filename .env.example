# ===========================================
# MISSION CONTROL - ENVIRONMENT VARIABLES
# ===========================================
#
# SECURITY WARNING:
# - NEVER commit the actual .env file to version control
# - NEVER share your API keys with anyone
# - NEVER paste API keys into chat messages
# - This file (.env.example) is safe to commit
# - The actual .env file is in .gitignore
#

# ------------------------------------------
# OpenRouter API Key (REQUIRED)
# ------------------------------------------
# Get your key at: https://openrouter.ai/keys
# Keys start with "sk-or-" and should be kept SECRET
#
OPENROUTER_API_KEY=sk-or-your-api-key-here

# ------------------------------------------
# Convex Database (OPTIONAL)
# ------------------------------------------
# Run `npx convex dev` to get your deployment URL
# This enables persistent task storage and agent coordination
#
CONVEX_URL=https://your-deployment.convex.cloud

# ------------------------------------------
# Model Configuration (OPTIONAL)
# ------------------------------------------
# Default AI model for agents
# Options: gpt-4o, gpt-4-turbo, gpt-3.5-turbo
#
DEFAULT_MODEL=gpt-4o

# ------------------------------------------
# Agent Scheduling (OPTIONAL)
# ------------------------------------------
# How often agents wake up (in minutes)
# Lower values = more API calls = higher cost
#
AGENT_INTERVAL=15

# ===========================================
# IMPORTANT SECURITY NOTES
# ===========================================
#
# After setting up your .env file:
#
# 1. Verify .env is ignored:
#    git status (should NOT show .env)
#
# 2. If you accidentally committed secrets:
#    - Immediately revoke/regenerate the exposed key
#    - Remove from git history (git filter-branch or BFG)
#    - Force push to overwrite history
#
# 3. Best practices:
#    - Use environment variables, not hardcoded values
#    - Rotate API keys periodically
#    - Monitor API usage for unexpected activity
#
